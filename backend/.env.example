# Server
PORT=3001
NODE_ENV=production

# JWT (gere hashes com node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=coloque-um-hash-de-64-caracteres-aqui
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_SECRET=outro-hash-de-64-caracteres
REFRESH_TOKEN_EXPIRES_IN=7d

# Zammad
ZAMMAD_URL=https://seu-zammad.com
ZAMMAD_API_TOKEN=seu-token-api

# CORS (URL exata do frontend no Vercel)
FRONTEND_URL=https://seu-frontend.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
LOGIN_RATE_LIMIT_MAX=10
